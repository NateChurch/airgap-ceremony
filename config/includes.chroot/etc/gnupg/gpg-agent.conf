pinentry-program /usr/bin/pinentry-curses

# Short cache lifetimes. This box is amnesiac but the session is not: an
# unattended terminal with a cached passphrase is the realistic failure mode.
default-cache-ttl 60
max-cache-ttl 300
default-cache-ttl-ssh 60
max-cache-ttl-ssh 300
